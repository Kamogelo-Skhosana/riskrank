pytest>=8.2
pytest-cov>=5.0
pytest-mock>=3.14
# ruff/black are pinned to the same versions as .pre-commit-config.yaml so
# local hooks and CI enforce identical rules. Bump both together.
ruff==0.16.8
black==26.5.1
pre-commit>=3.7
